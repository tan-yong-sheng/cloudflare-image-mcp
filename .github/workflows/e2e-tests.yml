name: E2E Tests

on:
  # Manual trigger for testing specific deployments
  workflow_dispatch:
    inputs:
      environment:
        description: 'Test environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production
          - local
      test_pattern:
        description: 'Test file pattern (e.g., tests/api/mcp/mcp-sdk.spec.ts)'
        required: false
        default: ''
        type: string
      custom_url:
        description: 'Custom Workers URL (optional - overrides auto-constructed URL)'
        required: false
        default: ''
        type: string

  # Run on PRs that modify packages/ or workers/
  pull_request:
    branches:
      - main
    paths:
      - 'packages/**'
      - 'workers/**'
      - 'e2e/**'
      - '.github/workflows/e2e-tests.yml'
      - 'package.json'

  # Run before publishing GHCR image
  workflow_call:
    inputs:
      environment:
        description: 'Test environment'
        required: false
        default: 'staging'
        type: string
      test_pattern:
        description: 'Test file pattern'
        required: false
        default: ''
        type: string

env:
  NODE_VERSION: '20'

jobs:
  e2e-tests:
    name: E2E Tests (${{ inputs.environment || 'staging' }})
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment || 'staging' }}
    permissions:
      contents: read
      checks: write
      pull-requests: write
    env:
      TEST_TARGET: ${{ inputs.environment || 'staging' }}
      # Use custom URL if provided, otherwise construct from account ID
      TEST_BASE_URL: ${{ inputs.custom_url != '' && inputs.custom_url || (inputs.environment == 'local' && 'http://localhost:3000' || format('https://cloudflare-image-workers{0}.{1}.workers.dev', inputs.environment == 'staging' && '-staging' || '', secrets.CLOUDFLARE_ACCOUNT_ID)) }}
      API_KEY: ${{ secrets.API_KEY }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify environment configuration
        run: |
          echo "Environment: ${{ env.TEST_TARGET }}"
          echo "Workers URL: ${{ env.TEST_BASE_URL }}"
          echo "API Key: ${{ secrets.API_KEY != '' && '✅ Configured' || '⚠️ Not configured (tests may fail if auth required)' }}"

          # For non-local environments with auto-constructed URL, verify CLOUDFLARE_ACCOUNT_ID is set
          if [ "${{ env.TEST_TARGET }}" != "local" ] && [ -z "${{ inputs.custom_url }}" ]; then
            if [ -z "${{ secrets.CLOUDFLARE_ACCOUNT_ID }}" ]; then
              echo "❌ ERROR: CLOUDFLARE_ACCOUNT_ID secret is not set"
              echo "This is needed to construct the Workers URL."
              echo "Either set CLOUDFLARE_ACCOUNT_ID or provide a custom_url."
              exit 1
            fi
            echo "✅ Workers URL auto-constructed from account ID"
          fi

          if [ -n "${{ inputs.custom_url }}" ]; then
            echo "✅ Using custom Workers URL"
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install E2E dependencies
        run: |
          cd e2e
          npm ci

      - name: Install Playwright browsers
        run: |
          cd e2e
          npx playwright install --with-deps chromium

      - name: Build local packages
        if: env.TEST_TARGET == 'local'
        run: |
          npm ci
          npm run build

      - name: Start local server
        if: env.TEST_TARGET == 'local'
        run: |
          cd packages/local
          npm run dev &
          npx wait-on http://localhost:3000/health --timeout 120000
        env:
          PORT: 3000
          NODE_ENV: test
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          S3_BUCKET: ${{ secrets.S3_BUCKET }}
          S3_ENDPOINT: ${{ secrets.S3_ENDPOINT }}
          S3_ACCESS_KEY: ${{ secrets.S3_ACCESS_KEY }}
          S3_SECRET_KEY: ${{ secrets.S3_SECRET_KEY }}
          S3_CDN_URL: ${{ secrets.S3_CDN_URL }}
          API_KEYS: ${{ secrets.API_KEY }}  # For local server auth

      - name: Run E2E tests
        run: |
          cd e2e
          if [ -n "${{ inputs.test_pattern }}" ]; then
            echo "Running specific test pattern: ${{ inputs.test_pattern }}"
            npx playwright test ${{ inputs.test_pattern }} --reporter=list,junit,html
          else
            echo "Running all E2E tests"
            npx playwright test --reporter=list,junit,html
          fi
        env:
          TEST_TARGET: ${{ env.TEST_TARGET }}
          TEST_BASE_URL: ${{ env.TEST_BASE_URL }}
          API_KEY: ${{ env.API_KEY }}
          CI: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ env.TEST_TARGET }}
          path: |
            e2e/playwright-report/
            e2e/test-results/
            e2e/junit-results.xml
          retention-days: 30

      - name: Upload screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-${{ env.TEST_TARGET }}-${{ github.run_id }}
          path: e2e/test-results/**/*.png
          retention-days: 7

      - name: Comment PR with test results
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = 'e2e/junit-results.xml';

            if (!fs.existsSync(path)) {
              console.log('No JUnit results found');
              return;
            }

            // Parse basic stats from JUnit XML
            const content = fs.readFileSync(path, 'utf8');
            const testsMatch = content.match(/tests="(\d+)"/);
            const failuresMatch = content.match(/failures="(\d+)"/);
            const errorsMatch = content.match(/errors="(\d+)"/);

            const tests = testsMatch ? parseInt(testsMatch[1]) : 0;
            const failures = failuresMatch ? parseInt(failuresMatch[1]) : 0;
            const errors = errorsMatch ? parseInt(errorsMatch[1]) : 0;
            const passed = tests - failures - errors;

            const status = failures === 0 && errors === 0 ? '✅ PASSED' : '❌ FAILED';

            const body = `## E2E Test Results ${status}

            **Environment:** ${process.env.TEST_TARGET}

            | Metric | Count |
            |--------|-------|
            | Total Tests | ${tests} |
            | Passed | ${passed} |
            | Failures | ${failures} |
            | Errors | ${errors} |

            <details>
            <summary>View Details</summary>

            - [View Playwright Report](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
            - Download artifacts from the Actions summary

            </details>
            `;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

      - name: Fail workflow if tests failed
        if: failure()
        run: |
          echo "E2E tests failed!"
          exit 1
