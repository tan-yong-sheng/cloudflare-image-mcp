{
  "name": "cloudflare-image-workers",
  "version": "0.1.0",
  "description": "Cloudflare Workers for Image Generation - OpenAI-compatible API + HTTP MCP + Frontend",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.896.0",
    "@modelcontextprotocol/sdk": "1.25.3",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "typescript": "^5.3.0",
    "wrangler": "4.60.0"
  }
}
